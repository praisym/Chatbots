describe('Verify the chatbot keeps the user engaged',() => {
    
    beforeEach(() => {
        cy.log('launch webrowser to the URL')        
        cy.visit('/')
    })

    it('Verify if the chatbot responses when the user is idle', () => {
        
    })


})